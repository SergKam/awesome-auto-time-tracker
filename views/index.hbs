<h1>awesome-auto-time-tracker</h1>
<h3>Active Windows</h3>
<form name="filter" action="/">
    SQL: <input placeholder="Enter SQL query" name="filter" value="{{filter}}">
</form>
<table>
    <caption>Log</caption>
    <thead>
    <tr>
        {{#each headers}}
            <th>{{this}}</th>
        {{/each}}
    </tr>
    </thead>
    <tbody>

    {{#each list}}
        <tr>
            {{#each this}}
                <td>{{this}}</td>
            {{/each}}
        </tr>
    {{/each}}
    </tbody>
</table>
<hr/>
